// ...
import { filter/*BS*/, debounceTime/*BE*/ } from 'rxjs';

@Component({ /* ... */ })
export class SearchComponent {
  input$ = new Subject<string>();

  constructor() {
    this.input$.pipe(
      filter(term => term.length >= 3)/*BS*/,
      debounceTime(500)/*BE*/
    )
    .subscribe(e => console.log(e));
  }
}
