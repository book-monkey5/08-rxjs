// ...
/*BS*/import { filter } from 'rxjs';/*BE*/

@Component({ /* ... */ })
export class SearchComponent {
  input$ = new Subject<string>();

  constructor() {
    this.input$/*BS*/.pipe(
      filter(term => term.length >= 3)
    )/*BE*/
    .subscribe(e => console.log(e));
  }
}
