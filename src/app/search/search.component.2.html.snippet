<input type="search"
  autocomplete="off"
  aria-label="Search"
  <!--BS-->[class.loading]="isLoading"<!--BE-->
  #searchInput
  (input)="input$.next(searchInput.value)">

<!--BS--><ul class="search-results" *ngIf="results.length">
  <li *ngFor="let book of results">
    <a [routerLink]="['/books', book.isbn]">
      {{ book.title }}
      <p>{{ book.subtitle }}</p>
    </a>
  </li>
</ul><!--BE-->
